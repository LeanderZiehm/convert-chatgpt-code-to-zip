version: "3.9"

services:
  markdown-zipper:
    build: .
    container_name: markdown-zipper
    env_file: .env
    ports:
      - "${APP_PORT:-8000}:8000"
    volumes:
      - .:/app
    restart: unless-stopped
